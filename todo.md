
# TODO:
最小化到托盘
居中用的是固定值， 没有获取到屏幕大小
增加一个读取配置文件的功能， 如果改了设置的时间， 每次就保存设置， 下次可以读取。
可以保存多个设置时间， 可以在界面中切换。

不知道为啥哦， 重设置已学时间后， 它就不自动更新了。 

# DONE:
在运行中点击继续 Tick, 会播放两个音频， 并且只能关掉一个， 另一个只能等它自己结束。 | *先 stop() 再继续*
停止 tick 之后，再继续， 已学时间会按着没停的时候继续更新 | *继续时设置一下 elapsed_time*
设置课时后， 下课时间和下节课时间没有更新。| *更新就行了*
本来 stop 可以继续的， 现在 stop 就把已学时间清零， 不能继续了。
多个音频播放时， 结束程序， 有线程没退出 (好像又复现不了， 没什么大问题， 先不用管了。 应该是错判， 其实程序没关，在后面没看见) 
播放音频时不能停止播放
播放音频会阻塞界面更新
启动居中
没有图标
